name: Java CI with Maven

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven
        
    - name: Build with Maven
      run: mvn -B clean install -Dmaven.test.skip=true

    - name: Package .archiplugin
      run: |
        mkdir -p target/package
        cp com.archimatetool.merge/target/com.archimatetool.merge-*.jar target/package/
        touch target/package/archi-plugin
        cd target/package && zip -r ../../duplicate-merge-plugin.archiplugin *

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: duplicate-merge-plugin.archiplugin
        path: duplicate-merge-plugin.archiplugin
